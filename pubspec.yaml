name: supply_chain
description: Supply Chain (SC) is a data flow and state management architecture.
version: 5.3.0
repository: https://github.com/ggsuite/supply_chain

environment:
  sdk: ">=3.10.0 <4.0.0"

# Add regular dependencies here.
dependencies:
  gg_once_per_cycle: ^1.3.5
  gg_fake_stopwatch: ^1.2.3
  gg_fake_timer: ^1.0.4
  collection: ^1.19.1
  meta: ^1.18.3

dev_dependencies:
  gg_is_github: ^1.0.6
  gg_log: ^1.0.6
  gg_cache: ^1.0.7
  lints: ^6.1.0
  test: ^1.31.2
  fake_async: ^1.3.3
  gg_golden: ^3.0.0

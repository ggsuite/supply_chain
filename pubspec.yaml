name: supply_chain
description: Supply Chain (SC) is a data flow and state management architecture.
version: 5.5.0
repository: https://github.com/ggsuite/supply_chain

environment:
  sdk: ">=3.13.0 <4.0.0"

# Add regular dependencies here.
dependencies:
  gg_once_per_cycle: ^1.4.0
  gg_fake_stopwatch: ^1.3.0
  gg_fake_timer: ^1.1.0
  collection: ^1.19.1
  meta: ^1.19.0

dev_dependencies:
  gg_is_github: ^1.1.0
  gg_log: ^1.1.0
  gg_cache: ^1.1.0
  lints: ^6.1.0
  test: ^1.31.2
  fake_async: ^1.3.3
  gg_golden: ^3.1.0
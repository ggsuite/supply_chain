# https://dart.dev/guides/libraries/private-files
# Created by `dart pub`
.dart_tool/

# Avoid committing pubspec.lock for library packages; see
# https://dart.dev/guides/libraries/private-files#pubspeclock.
pubspec.lock

node_modules
coverage
build
*.vm.json
.DS_Store
# Ignore .gg contents but keep the check results. The pattern must be
# `.gg/*`, not `.gg`: git cannot re-include a file whose parent directory
# is excluded, so a negation below a `.gg` line would be dead.
.gg/*
!.gg/.gg.json
